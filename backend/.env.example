# Server
PORT=3000

# CORS — comma-separated origins allowed to call the API
CORS_ORIGIN=http://localhost:4200

# Deploy (optional — only needed if you use the deploy feature)
GITHUB_TOKEN=
NETLIFY_TOKEN=
